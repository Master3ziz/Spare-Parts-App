<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.car_spares">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.MassiveAdmin">


        <activity android:name=".activities.SelectionActivity"/>

        <activity android:name="com.app.car_spares.activities.PartDetailsActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.ViewOrdersHistoryActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.ViewOrdersActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.AddPartActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.ProfileActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.SignUpActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.SignInActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.ViewPartsActivity"
            android:exported="true"/>

        <activity android:name="com.app.car_spares.activities.CustomerActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan"/>

        <activity android:name="com.app.car_spares.activities.SellerActivity"
            android:exported="true"/>

        <activity
            android:name="com.app.car_spares.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>